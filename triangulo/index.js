let base = parseInt(prompt("Digire la base del triangulo"));
let altura = parseInt(prompt("Digite la altura del triangulo"));
let operacion = base * altura
let r = operacion / 2
alert("El area del triangulo es: " + r)